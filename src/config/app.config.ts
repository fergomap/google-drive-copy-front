interface AppConfig {
    ROUTES: {
        LOG_IN: string;
        HOME: string;
    }
}

export const APP_CONSTANTS: AppConfig = {
    ROUTES: {
        LOG_IN: '/log-in',
        HOME: '/home'
    }
};
